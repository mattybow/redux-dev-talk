import React from 'react';

export default class Instructions{
	render(){
		return <div className="instructions">
			<span><i>⌘ + click to add blocks, spacebar to clear highlights, normal click on action triggers flow</i></span>
		</div>
	}
}